services:
  postgres:
    image: postgres:16-alpine
    container_name: aws-demo-001-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: aws_demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d aws_demo"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  pgadmin:
    image: dpage/pgadmin4:9.10
    container_name: aws-demo-001-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: "False"
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: "False"
    ports:
      - "127.0.0.1:5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
      - ./docker/pgadmin/servers.json:/pgadmin4/servers.json:ro
    depends_on:
      postgres:
        condition: service_healthy

  profile-service:
    build:
      context: ./apps/profile-service
    restart: unless-stopped
    environment:
      CORS_ORIGIN: http://localhost:3001
      DATABASE_HOST: postgres
      DATABASE_MAX_CONNECTIONS: "5"
      DATABASE_NAME: aws_demo
      DATABASE_PASSWORD: postgres
      DATABASE_PORT: "5432"
      DATABASE_SSLMODE: disable
      DATABASE_USERNAME: postgres
      PORT: "8080"
    ports:
      - "127.0.0.1:8080:8080"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
    name: aws-demo-001-postgres-data
  pgadmin_data:
    name: aws-demo-001-pgadmin-data
