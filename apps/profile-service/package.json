{
  "name": "profile-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "go build -o dist/profile-service ./cmd/server",
    "check-types": "go vet ./...",
    "dev": "go run ./cmd/server",
    "test": "go test ./..."
  }
}
